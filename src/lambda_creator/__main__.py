"""
Main entry point for the Lambda Creator package.

This module allows the package to be run as a script using `python -m src.lambda_creator`.
"""

from .cli import main

if __name__ == "__main__":
    main()
